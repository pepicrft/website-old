title = "~/ppinera.es/src"
description = "Thoughts, reflections, and ideas"

# The URL the site will be built for
base_url = "https://ppinera.es"
theme = "lightspeed"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

[markdown]
highlight_code = true

[slugify]
paths = "on"

[extra]
author = "Pedro Piñera"
zola = true
netlify = true
maintained_with_love = true
footer_tagline = "Build developer experiences that spark joy"
footer_links = [
    {url = "$BASE_URL/pages/about", name = "About"},
    {url = "$BASE_URL/atom.xml", name = "RSS"},
]